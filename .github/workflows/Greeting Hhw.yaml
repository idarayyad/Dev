name: Greeting Hhw

on:
    workflow_dispatch:
        inputs:
            username:
                description: 'The username of the user
                required: true
            favorite_color:
                description: 'The favorite color of the user'
                required: true

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Output Inputs
            run: |
            echo "Inputs: Username - ${{github.event.inputs.Ibrahim}}, Favorite Color"

    job2:
        runs-on: ubuntu-latest
        needs: job1
        steps:
            - name: Use Output from job 1
              run: |
               GREETING="Hello, ${{ needs.job1.outputs.username }}! Your favorite color is "

    job3:
        runs-on: ubuntu-latest
        needs: job2
        steps:
            - name: Echo greeting from job 2
            run: |
             echo ${{ needs.job2.outputs.GREETING }}

    